# Server
PORT=4000
NODE_ENV=development

# App / project
APP_NAME=mpange-backend-mongo
FILE_UPLOAD_DIR=uploads

# MongoDB
# For local dev: mongodb://localhost:27017/mpange_dev
# For Atlas: mongodb+srv://<user>:<password>@cluster0.xyz.mongodb.net/mpange_prod?retryWrites=true&w=majority
MONGO_URI=mongodb+srv://luchanda95_app_db:chanda@cluster0.md06per.mongodb.net/mpange_dev?retryWrites=true&w=majority

# JWT
JWT_SECRET=t7lTdOp7SiVf3p4xShphpq85D8CiakCKMjdRR5YdIvYiN7KylLhd/DK+FcWly55m
4eXVh7BeqT78U2xq8rKAQw==
# Expiration for access tokens (jwt sign `expiresIn`). Example: '7d', '24h', or '3600s'
JWT_EXPIRES_IN=7d

# Refresh token settings (optional)
REFRESH_TOKEN_EXPIRES_DAYS=30
# If you implement refresh tokens, consider hashing stored tokens and rotating them.

# CORS / Allowed origin for web clients (set to your frontend origin in production)
# For development with Flutter web or local frontends:
CORS_ORIGIN=http://localhost:5151
# or use specific LAN IP when testing from device:
# CORS_ORIGIN=http://192.168.1.100:3000

# OAuth / Social logins
# Google client ID used to verify ID tokens server-side
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Apple Sign In
# For production you'd use Apple Service ID / key id / team id / private key path
APPLE_CLIENT_ID=com.example.app
APPLE_KEY_ID=YOUR_APPLE_KEY_ID
APPLE_TEAM_ID=YOUR_APPLE_TEAM_ID
APPLE_PRIVATE_KEY_PATH=./certs/apple_private_key.p8

# Optional: frontend URL used for emails/links
FRONTEND_URL=http://localhost:3000

# Logging & debugging
LOG_LEVEL=debug

# Optional files / storage
# If using S3 or GCS for uploads, set those keys instead of local upload dir
USE_S3=false
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY=
S3_SECRET_KEY=
